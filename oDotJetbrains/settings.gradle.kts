plugins {
    // Lets Gradle auto-provision a JDK 17 toolchain if one is not already present.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "oDotJetbrains"
