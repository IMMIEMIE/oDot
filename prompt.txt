1、工具调用 搜索的时候老是报错：stream did not contain valid UTF-8做一下UTF-8/GBK编码的适配；
2、现在执行任务的时候是开一个新会话去执行，这样不对，应该是在当前会话下继续执行，但是模式切换到Agent模式；
3、计划执行过程中Timeline的显示方法不对，应该是直接内嵌到timelinePane当中，与其他timeline块一样，只是显示样式不同而已；
4、变更快照现在不会显示添加/删除了多少行代码，并分别用绿色、红色显示，加上这个功能；
5、命令确认的时候，除了接受/拒绝，还应当有一个接受并加入白名单的按钮（项目里面已经有自动命令-白名单功能的实现了）